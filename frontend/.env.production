# Prod build default: empty means the client calls same-origin relative
# paths (/api/v1/...), which nginx proxies to the backend (see
# backend/deploy/nginx/diskadvisor.conf). Only set this to an absolute URL
# if the frontend and API are served from different origins.
VITE_API_BASE_URL=
